# `spec_generator.py`

_Unable to parse module for summary._

## Usage

Import the module from the `project` package. Example:

```python
from project import spec_generator
# or
import project.spec_generator
```
